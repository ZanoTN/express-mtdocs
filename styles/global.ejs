<style>
  :root {
    --bs-border-radius: 0rem;
    --bs-border-radius-sm: 0rem;
    --bs-border-radius-lg: 0rem;
    --bs-border-radius-xl: 0rem;
    --bs-border-radius-xxl: 0rem;
    --bs-border-radius-2xl: 0rem;
  }
  pre {
    background-color: var(--bs-secondary-bg);
    padding: 1rem;
    overflow-x: auto;
  }
  code {
    background-color: var(--bs-secondary-bg);
  }
  table {
    border-collapse: collapse;
    width: 100%;
  }
  table, th, td {
    border: 1px solid var(--bs-border-color); /* adapts to theme */
  }
  thead {
    background-color: var(--bs-secondary-bg); /* adapts to theme */
    color: var(--bs-body-color);
  }
  th, td {
    padding: 0.5rem;
    vertical-align: middle;
  }
  @media (min-width: 768px) {
    .vh-md-100 {
      height: 100vh !important;
    }
    .menu-section-width {
      width: 300px; 
      flex: 0 0 300px;
    }
  }
  .icon-menu {
    width: 1em;
    height: 1em;
    text-align: center;
    fill: currentColor;
  }
</style>